{
  "name": "quince-invitacion",
  "version": "1.0.0",
  "private": true,
  "description": "Invitación de XV años + API de confirmaciones (Vercel Functions)",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev:local": "node dev-server.js",
    "deploy": "vercel --prod",
    "lint": "eslint api lib dev-server.js test",
    "test": "node --test test/**/*.test.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "eslint": "^10.8.0",
    "vercel": "^58.1.0"
  }
}
